# Installation

To install the package from github via pip run

```
pip install git+https://github.com/hummerichsander/hydrantic.git
```

Alternatively, you can clone the package

```bash
git clone git@github.com:hummerichsander/hydrantic.git
```

then navigate to the repository's directory and run

```
pip install -e .
```
